#This is a correct input example
#Every line beginning with '#' is a comment
#Error occurs only for wrong keys
#kase-sensitive
#------------------------------------------------

#Protein code and desired file type, possible values 'cif' or 'pdb'
#'cif' is for PDBx/mmCIF
#'pdb' for standard PDB file (default)

protein = 2f3z
protein_filetype = pdb

#the model to take from pdb or mmcif, if omitted all of them will be taken
PDB_model = 1

#The ph at which the hydrogens sahll be added to the protein
#default = 7.0
ph = 7.0

#with wich tool the pdb shall be fixed
#adding missing atoms, missing hydrogens and substituting
#non standard residues with standard ones
#default is pdbfixer (needs openmm and conda environment)

repairing_method = pdbfixer

#local tells if the program shall use a pdb (or mmcif) file already
#on the computer
#if 'no' (default) it will download it from the wwPDB database
#if 'yes' insert the absolute path in filepath
#any keyword different from yes and no will abort the program

local = no
#filepath = None

#if ligand_in_protein = yes (default) ligand will be taken from the
#protein pdb file, taking all the atoms with th PDB identifier
#ligand = PDB identifier(s) (es HOH, HETATM, DOD)
#if ligand is omitted will take all not water not protein atoms (at your own risk)

#if ligand_in_protein = no ligand will be given as input
#ligand = SMILES or path/to/PDB
#the ligand can be given as a pdb file (I suggest to use absolute path) or as a SMILES

ligand_in_protein = yes
ligand = HOH CA

#The program with wich elaborate the ligand (optimization and potential)
#default primadorac

ligand_elaboration_program = primadorac
